# Quality Harness

The harness owns schema validation, token lint, PPTX inspection, preview rendering, visual regression, and text overflow checks.

First-version implementations favor stable interfaces over complete rendering. Visual diff, preview rendering, and overflow checks are stubs until a rendering environment is selected.
