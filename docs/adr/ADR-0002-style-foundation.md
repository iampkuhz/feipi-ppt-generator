# ADR-0002: Style Foundation

## Decision

All visual values in specs and component props must use Layer0 aliases.

## Consequences

Raw colors, font sizes, radius values, margins, and unknown icon paths are blocked by validation and token lint.
