# ADR-0003: Renderer Backend

## Decision

Use `pptxgenjs` as the first renderer backend behind a generic renderer interface.

## Consequences

The first implementation can generate PPTX files while preserving the option to add other backends later.
