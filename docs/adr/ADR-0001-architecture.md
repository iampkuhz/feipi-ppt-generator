# ADR-0001: Architecture

## Decision

Use a schema-first TypeScript architecture with component registry, style foundation, renderer interface, template packs, and QA harness.

## Consequences

The repository can evolve by adding registered components and OpenSpec-style changes without tying core code to a specific template.
