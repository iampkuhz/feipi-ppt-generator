# ppt-lord-visual-qa

## Purpose

Generate previews, run QA checks, and produce a report.

## Workflow

1. Validate the deck spec.
2. Generate PPTX.
3. Run `render-preview`.
4. Run `qa`.

Preview rendering and visual diff are first-version stubs.
