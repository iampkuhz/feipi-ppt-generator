# Component Author Skill

适用场景：新增或修改组件。

输入要求：组件边界、props schema、示例和目标 layer。

调用步骤：创建 OpenSpec change，修改组件、registry、示例和测试，运行最小验证。

禁止事项：临时发明未知组件或传入 raw 视觉值。

输出产物：组件源码、registry entry、example spec、测试和 QA report。

失败处理：缺少任一必备产物时标记未完成。
