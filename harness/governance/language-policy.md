# 中文规约

项目内容默认中文，包括文档、注释、测试描述、schema 说明、运行日志、QA report 和 agent 输出。

允许英文范围：目录名、文件名、代码标识符、类型名、函数名、类名、变量名、包名、命令、路径、API 名称和标准术语词组。

agent、skill、hook 配置也适用本规约。运行时入口可以保留英文 name、description 中的标准术语、命令和路径，但说明正文必须优先使用中文。

新增文档时应采用渐进式加载原则：先写清适用场景、最小上下文、禁止范围、验证命令和失败处理，不把完整仓库规则复制到每个入口文件。
